export const comp = {
  'menor que': (each, value) => +each < +value,
  'maior que': (each, value) => +each > +value,
  'igual a': (each, value) => +each === +value,
};

export const opt = [
  'population',
  'orbital_period',
  'diameter',
  'rotation_period',
  'surface_water',
];

export const SAI_LINT = 'vaza';
